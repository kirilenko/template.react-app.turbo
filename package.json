{
  "name": "template.react-app.turbo",
  "version": "0.0.0",
  "description": "A template for creating React applications with Turbo",
  "author": "Kirilenko",
  "license": "MIT",
  "scripts": {
    "clean": "turbo clean && rm -rf node_modules",
    "setup": "./scripts/nvm-use.sh && pnpm i && pnpm audit",
    "dev": "dotenv -e ./.env -- turbo dev",
    "format": "turbo format",
    "build": "turbo build --filter=...[apps/web-client]",
    "preview": "dotenv -e ./.env -- turbo preview --filter=...[apps/web-client]"
  },
  "devDependencies": {
    "dotenv-cli": "^7.4.2",
    "prettier": "2",
    "process": "^0.11.10",
    "turbo": "^2.0.4"
  },
  "packageManager": "pnpm@9.3.0",
  "eslintIgnore": [
    "**/dist/*",
    "**/node_modules/*",
    "**/.turbo/**"
  ]
}
